namespace ZincFramework
{
    namespace Events
    {
        public enum EventType
        {
            SceneLoading,

            ConnectFailed,
            ServerReject,
            IsReconnecting,
            Reconnected,
        }
    }
}
